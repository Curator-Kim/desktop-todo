{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow the settings window to check and install signed updates",
  "windows": ["settings"],
  "permissions": ["updater:default"]
}
